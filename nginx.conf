server {
    listen 443;
    server_name amazon-jp-price-ai.sotatek.works;

    root /var/www/amazon-ai-price;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    # Cache configuration for static files
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg)$ {
        expires 30d;
        add_header Cache-Control "public, no-transform";
    }

    # Security headers
    add_header X-Frame-Options "SAMEORIGIN";
    add_header X-XSS-Protection "1; mode=block";
    add_header X-Content-Type-Options "nosniff";
}

# server {
#     listen 443 ssl;
#     server_name amazon-jp-price-ai.sotatek.works;
#
#     # SSL Configuration (Update these paths with your actual certificate files)
#     ssl_certificate /etc/letsencrypt/live/amazon-jp-price-ai.sotatek.works/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/amazon-jp-price-ai.sotatek.works/privkey.pem;
#
#     # Optimized SSL settings
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#
#     root /var/www/amazon-ai-price;
#     index index.html;
#
#     location / {
#         try_files $uri $uri/ /index.html;
#     }
#
#     # Cache configuration for static files
#     location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg)$ {
#         expires 30d;
#         add_header Cache-Control "public, no-transform";
#     }
#
#     # Security headers
#     add_header X-Frame-Options "SAMEORIGIN";
#     add_header X-XSS-Protection "1; mode=block";
#     add_header X-Content-Type-Options "nosniff";
# }


